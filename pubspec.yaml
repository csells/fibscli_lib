name: fibscli_lib
description: A starting point for Dart libraries or applications.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  socket_io_client: ^0.9.11
  logging: ^0.11.4

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

